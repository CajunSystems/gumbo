<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.cajunsystems</groupId>
    <artifactId>gumbo</artifactId>
    <version>0.2.0</version>
    <packaging>jar</packaging>

    <name>Shared Log</name>
    <description>
        A shared log with pluggable persistence adapters, used as a source-of-truth ledger.
        Stateless executors read state from the log, process it, and append back.
        Inspired by the Boki shared-log architecture (SOSP 2021).
    </description>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <slf4j.version>2.0.12</slf4j.version>
        <logback.version>1.5.3</logback.version>
        <junit.version>5.10.2</junit.version>
        <assertj.version>3.25.3</assertj.version>
        <awaitility.version>4.2.1</awaitility.version>
        <kryo.version>5.6.0</kryo.version>
        <fdb.version>7.3.43</fdb.version>
    </properties>

    <dependencies>
        <!-- Logging API -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>

        <!-- Logging impl (test + runtime) -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <version>${awaitility.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Kryo binary serialization (for TypedLogView) -->
        <dependency>
            <groupId>com.esotericsoftware</groupId>
            <artifactId>kryo</artifactId>
            <version>${kryo.version}</version>
        </dependency>

        <!-- FoundationDB Java client (optional persistence / sequencer backend) -->
        <dependency>
            <groupId>org.foundationdb</groupId>
            <artifactId>fdb-java</artifactId>
            <version>${fdb.version}</version>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!--
                Mutation testing. A passing test suite says the tests ran; it does not say
                they would have failed had the code been wrong. Several tests in this repo
                did pass against the very defect they were written for, so that gap is
                checked rather than assumed.

                Scoped to persistence + service: that is where the durability and
                concurrency invariants live, and where every defect found in review landed.
                Run with `mvn test-compile org.pitest:pitest-maven:mutationCoverage`.
            -->
            <plugin>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-maven</artifactId>
                <version>1.16.1</version>
                <dependencies>
                    <dependency>
                        <groupId>org.pitest</groupId>
                        <artifactId>pitest-junit5-plugin</artifactId>
                        <version>1.2.1</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <targetClasses>
                        <param>com.cajunsystems.gumbo.persistence.*</param>
                        <param>com.cajunsystems.gumbo.service.*</param>
                    </targetClasses>
                    <targetTests>
                        <param>com.cajunsystems.gumbo.*</param>
                    </targetTests>
                    <excludedClasses>
                        <!-- Needs a live FDB cluster; its tests skip, so every mutant survives. -->
                        <param>com.cajunsystems.gumbo.persistence.FoundationDBPersistenceAdapter*</param>
                    </excludedClasses>
                    <outputFormats>
                        <param>HTML</param>
                        <param>XML</param>
                    </outputFormats>
                    <timestampedReports>false</timestampedReports>
                    <threads>4</threads>
                    <!--
                        Ratchet, not a target: set to the measured score, so a decline
                        fails the build rather than being absorbed by slack.

                        Baseline at introduction: 364/486 killed, test strength 81%.
                        PIT rounds, so the real headroom is about two mutants — 363 still
                        reports 75 and passes, 362 reports 74 and fails. Observed
                        run-to-run variance is one mutant (363-364), from timing-sensitive
                        tests in the service package; a slower machine tends to kill more,
                        not fewer, since a timed-out mutant counts as killed. Raise this
                        when the score rises.
                    -->
                    <mutationThreshold>75</mutationThreshold>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.12.1</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
            </plugin>
        </plugins>
    </build>
</project>
