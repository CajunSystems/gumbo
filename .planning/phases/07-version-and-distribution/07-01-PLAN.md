---
phase: 07-version-and-distribution
plan: 07-01
type: execute
---

<objective>
Bump pom.xml to 0.2.0, add JitPack distribution config and installation instructions, and create CHANGELOG.md documenting all Phase 1–6 changes.

Purpose: Prepare gumbo 0.2.0 for distribution via JitPack. Consumers add a single repository + dependency block to their Maven/Gradle build. The CHANGELOG documents what changed vs. the earlier pre-0.2.0 state.
Output: pom.xml at 0.2.0, jitpack.yml, README updated (Installation section + TOC entry + package cmd fix), CHANGELOG.md.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/ROADMAP.md

**Repo**: https://github.com/CajunSystems/gumbo
**JitPack coordinates** (auto-derived from GitHub org + artifact):
- groupId: `com.github.CajunSystems`
- artifactId: `gumbo`
- version: `0.2.0` (after tagging the commit)

**pom.xml current state**:
- `<groupId>com.cajunsystems</groupId>` (internal; JitPack overrides with `com.github.CajunSystems`)
- `<artifactId>gumbo</artifactId>`
- `<version>1.0.0-SNAPSHOT</version>` → bump to `0.2.0`

**jitpack.yml required for Java 21**: JitPack defaults to Java 8/11. Without a `jitpack.yml` specifying `openjdk21`, the build will fail.

**README current state**:
- TOC ends at item 13: `[Building](#building)` (line ~22)
- Building section (line ~878): mentions `sharedlog-1.0.0-SNAPSHOT.jar` — fix to `gumbo-0.2.0.jar`
- No Installation section exists yet

**Changes in 0.2.0 (from git log)**:
- `feat(02-01)` + `feat(02-02)`: `getLatestSeqnumForTag()` on PersistenceAdapter; `getLatestSeqnum()` on LogView + DefaultLogView — O(1) across all 4 adapters
- `feat(03)`: `subscribeTail(listener)` — verified pre-existing and correct
- `feat(04-01)`: `setValue/getValue/deleteValue` KV API on PersistenceAdapter, LogView, DefaultLogView
- `feat(05-01)`: KV in InMemory (ConcurrentHashMap) + FileBased (kv.dat append-log, durable across reopen)
- `feat(05-02)`: KV write-through in Batching; kvSubspace in FDB
- `feat(06-01)`: `getLatestSeqnum`, `currentPosition`, `setValue/getValue/deleteValue` on TypedLogView + DefaultTypedLogView
- `feat(06-02)`: ActorCheckpointExample + README "Actor checkpoints" section
- `feat(01-01)`: InMemory `readByTag` linear scan → tailMap (O(log N))
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump pom.xml version to 0.2.0 and fix README package command</name>
  <files>
    pom.xml
    README.md
  </files>
  <action>
    1. In `pom.xml`, change:
       ```xml
       <version>1.0.0-SNAPSHOT</version>
       ```
       to:
       ```xml
       <version>0.2.0</version>
       ```

    2. In `README.md`, in the Building section, fix the package command comment:
       Change: `mvn package         # produce sharedlog-1.0.0-SNAPSHOT.jar`
       To:     `mvn package         # produce gumbo-0.2.0.jar`
  </action>
  <verify>grep -n "version" pom.xml | head -5</verify>
  <done>pom.xml shows `<version>0.2.0</version>`; README package cmd references gumbo-0.2.0.jar</done>
</task>

<task type="auto">
  <name>Task 2: Create jitpack.yml and add Installation section to README</name>
  <files>
    jitpack.yml
    README.md
  </files>
  <action>
    1. Create `jitpack.yml` at repo root:
       ```yaml
       jdk:
         - openjdk21
       ```

    2. In `README.md`, update the Table of Contents — insert item 13 before current item 13 (Building):
       Change the TOC from:
       ```
       13. [Building](#building)
       ```
       To:
       ```
       13. [Installation](#installation)
       14. [Building](#building)
       ```

    3. In `README.md`, insert a new `## Installation` section immediately BEFORE `## Building`:
       The exact text to find (anchor): `## Building`
       Insert before it (including the `---` separator):

       ```markdown
       ## Installation

       Gumbo is distributed via [JitPack](https://jitpack.io). No local build required.

       ### Maven

       Add the JitPack repository to your `pom.xml`:

       ```xml
       <repositories>
           <repository>
               <id>jitpack.io</id>
               <url>https://jitpack.io</url>
           </repository>
       </repositories>
       ```

       Then add the dependency:

       ```xml
       <dependency>
           <groupId>com.github.CajunSystems</groupId>
           <artifactId>gumbo</artifactId>
           <version>0.2.0</version>
       </dependency>
       ```

       ### Gradle

       ```groovy
       repositories {
           maven { url 'https://jitpack.io' }
       }

       dependencies {
           implementation 'com.github.CajunSystems:gumbo:0.2.0'
       }
       ```

       > **Note**: Gumbo requires Java 21+.

       ---

       ```

    IMPORTANT: Use the Edit tool, not a full rewrite. Match exact whitespace from the file.
    The `## Building` heading appears at line ~878. The anchor text is `\n## Building\n`.
    Insert the Installation section including its trailing `---\n\n` separator before the existing `## Building` heading.
  </action>
  <verify>grep -n "Installation\|jitpack\|JitPack\|CajunSystems" README.md | head -15</verify>
  <done>jitpack.yml exists with openjdk21; README has Installation section with Maven+Gradle snippets; TOC has 14 items</done>
</task>

<task type="auto">
  <name>Task 3: Create CHANGELOG.md</name>
  <files>
    CHANGELOG.md
  </files>
  <action>
    Create `CHANGELOG.md` at repo root following the [Keep a Changelog](https://keepachangelog.com) format:

    ```markdown
    # Changelog

    All notable changes to gumbo are documented here.
    Format: [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

    ---

    ## [0.2.0] — 2026-04-17

    ### Added

    **Efficient positional reads**
    - `LogView.readFrom(LogPosition, int)` — read entries starting from a specific seqnum; O(log N) in InMemory (fixed from linear scan), O(seek) in FileBased/FDB/Batching
    - `LogPosition` constructor accepting a seqnum for checkpoint-based replay

    **Per-tag latest seqnum**
    - `LogView.getLatestSeqnum()` — returns the highest seqnum written to a tag's log in O(1) via maintained metadata; available across all 4 persistence adapters
    - `PersistenceAdapter.getLatestSeqnumForTag(LogTag)` — backing method with O(log N) implementation in InMemory/FileBased, O(1) via AtomicLong cache in FDB, merged-scan in Batching

    **Push-tail subscribe**
    - `LogView.subscribeTail(listener)` — delivers only future (live) entries with no backlog; enables seamless catching-up → live handoff without polling

    **Per-tag key-value store**
    - `LogView.setValue(String key, byte[] value)` — persist a durable KV checkpoint scoped to the tag
    - `LogView.getValue(String key)` — read a KV checkpoint
    - `LogView.deleteValue(String key)` — remove a KV entry
    - Durability per adapter: FileBased uses an append-only `kv.dat` sidecar file (survives reopen); FDB uses a dedicated `kv` subspace; Batching is write-through to delegate; InMemory is in-memory only

    **TypedLogView additions**
    - `TypedLogView.getLatestSeqnum()` — delegating wrapper of `LogView.getLatestSeqnum()`
    - `TypedLogView.currentPosition()` — default method returning `LogPosition.BEGINNING` when the view is empty
    - `TypedLogView.setValue/getValue/deleteValue` — KV API delegated to underlying `LogView`

    **Examples and documentation**
    - `ActorCheckpointExample` — runnable JUnit 5 example demonstrating: read KV checkpoint → replay backlog via `readFrom` → switch to live via `subscribeTail` → persist new checkpoint on each entry
    - README "Actor checkpoints" section documenting all new APIs with code snippet

    ### Changed

    - `pom.xml` version bumped from `1.0.0-SNAPSHOT` to `0.2.0`
    - `DefaultLogView.getLatestSeqnum()` reimplemented to call `PersistenceAdapter.getLatestSeqnumForTag()` instead of materialising all entries (O(N) → O(1))

    ---
    ```

    Write the file at the repo root: `/CHANGELOG.md`
  </action>
  <verify>head -5 CHANGELOG.md</verify>
  <done>CHANGELOG.md exists at repo root with [0.2.0] section covering all Phase 1-6 changes.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `pom.xml` `<version>` is `0.2.0`
- [ ] `jitpack.yml` exists with `openjdk21`
- [ ] README TOC has Installation entry (14 items total)
- [ ] README has `## Installation` section before `## Building` with Maven + Gradle snippets using `com.github.CajunSystems:gumbo:0.2.0`
- [ ] README Building section references `gumbo-0.2.0.jar`
- [ ] `CHANGELOG.md` exists with `[0.2.0]` entry covering readFrom, getLatestSeqnum, subscribeTail, KV, TypedLogView additions
- [ ] `mvn test -q` still passes (pom change must not break anything)
</verification>

<success_criteria>
- Consumers can add JitPack repo + `com.github.CajunSystems:gumbo:0.2.0` dependency and use gumbo
- CHANGELOG.md accurately documents all 0.2.0 additions
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-version-and-distribution/07-01-SUMMARY.md`:

# Phase 7 Plan 1: Version + Distribution Summary

**[Substantive one-liner]**

## Accomplishments

## Files Created/Modified

- `pom.xml` — version bumped to 0.2.0
- `jitpack.yml` — Java 21 build spec
- `README.md` — Installation section + TOC update + package cmd fix
- `CHANGELOG.md` — new file documenting all 0.2.0 changes

## Decisions Made

## Issues Encountered

## Next Step

Phase 7 complete.
</output>
